# 网页版图片云端同步（飞书 → 云存储 → 浏览器）

## 目标

创作知识库 / 车型知识库的图片**不再由本机上传、不再需要登录飞书**：
云端服务持飞书自建应用凭据，按需从飞书拉图并缓存，网页版直读。

## 为什么不能"网页直连飞书"

| 门槛 | 实测结论 |
| --- | --- |
| 匿名可读 | ❌ 实测 `https://<租户>.feishu.cn/wiki/<token>` 匿名访问 → 302 跳 `accounts.feishu.cn/accounts/page/login`。飞书的"公开"≠ 匿名可下载 |
| 需要 token | 飞书 OpenAPI 要 `tenant_access_token`，换取凭据是 `app_id` + `app_secret`，**不能放进网页** |
| 跨域 | 飞书接口不给浏览器发 CORS 头，网页直连必被拦 |

所以"去飞书取图"这一步只能放在云端，浏览器只认云端返回的字节。

## 架构

```
浏览器 → CloudBase 云函数 feishu-media（HTTP 访问服务）
             ├─ 命中缓存 → 云存储直出
             └─ 未命中   → 飞书 OpenAPI 下载 → 写云存储 → 返回
```

- 云函数：`cloudfunctions/feishu-media/index.js`
- 缓存：CloudBase 云存储，`feishu-media/<slot>/<车型>/<文件名>`
- 前端：配 `VITE_MEDIA_BASE` 后，图片地址全部指向云函数域名

## 你需要做的一次性动作（约 10 分钟）

### 1. 飞书开放平台建自建应用

1. 打开 https://open.feishu.cn/app → **创建企业自建应用**
2. 「权限管理」开通：
   - `drive:drive`（云文档）或至少 `drive:file:readonly`
   - `wiki:wiki:readonly`（知识库节点读取）
   - `docx:document:readonly`（文档块读取，用于解析图片）
3. 「版本管理与发布」→ 创建版本 → **申请发布**（个人版一般自动通过）
4. 「凭证与基础信息」里复制 **App ID** 和 **App Secret**

> 如果应用读不到知识库：把应用加为知识库成员，或在知识库设置里把可见范围放开到"企业内所有人"。

### 2. 把凭据给我（或自己填进控制台）

在 CloudBase 控制台 → 云函数 → `feishu-media` → **配置 → 环境变量** 填：

| 变量 | 值 |
| --- | --- |
| `FEISHU_APP_ID` | 飞书 App ID |
| `FEISHU_APP_SECRET` | 飞书 App Secret |
| `SITE_BASE` | `https://zhiji-d4g0etkrwf7e7d1de-1494775572.tcloudbaseapp.com` |
| `SYNC_TOKEN` | 自定义口令（定时同步时用） |
| `CACHE_TTL` | 缓存秒数，默认 2592000（30 天） |

### 3. 部署并重新构建站点

```bash
npm run deploy:media-fn                      # 部署云函数（首次要带 --httpFn，脚本已带）
VITE_MEDIA_BASE=https://<环境>.service.tcloudbase.com/feishu-media npm run deploy:cloudbase
```

云函数的 HTTP 访问域名在控制台 → 云函数 → `feishu-media` → HTTP 访问服务里能看到。

### 4. 预热 + 定时刷新

- 手动预热：`https://<环境>.service.tcloudbase.com/feishu-media/sync?token=<SYNC_TOKEN>`
- 定时：控制台 → 云函数 → 触发器 → 定时触发器，Cron `0 0 4 * * * *`（每天凌晨 4 点），入参 `{"mode":"sync","token":"<SYNC_TOKEN>"}`

## 验证

```bash
# 健康检查（看凭据是否配好）
curl "https://<环境>.service.tcloudbase.com/feishu-media/health"
# → {"ok":true,"configured":true,"siteBase":"https://..."}

# 取一张图
curl -o /tmp/a.jpg -w "%{http_code} %{content_type}\n" \
  "https://<环境>.service.tcloudbase.com/feishu-media/feishu-creation-materials/智己L6/image.png"
```

## 回退

不配 `VITE_MEDIA_BASE` 就自动回到"图片随站点发布"的模式，两条路可以随时切。

## 安全

- `app_secret` 只存在于云函数环境变量里，**不进代码库、不进网页**
- 站点目前公网可访问，图片也一样；建议配 CloudBase 访问控制（见「部署网页版到腾讯云.md」）
