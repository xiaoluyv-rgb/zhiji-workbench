# 智己运营工作台 · 团队分发与使用说明

> 团队分发主路径：**GitHub 公开仓库 + WorkBuddy 自行安装**（同事复制一段 prompt 给 WorkBuddy，自动 clone + install + 填 Key + 启动）。
> 备选：解压即用的一键运行包、双击图标的桌面安装版（见末尾「进阶」）。

---

## 一、同事 30 秒装好（推荐路径）

维护者先把代码推到 GitHub（一次性操作），同事只要：

1. 在 WorkBuddy 对话框粘贴 [INSTALL_FOR_WORKBUDDY.md](./INSTALL_FOR_WORKBUDDY.md) 里那段「复制即用」提示词
2. WorkBuddy 自动：`git clone` → `npm install` → 询问并写入 DeepSeek Key → `npm run dev` → 浏览器自动打开 `http://localhost:5173`
3. 关掉 WorkBuddy 对话也行——dev server 跑在同事自己电脑上

> 路径建议放 `C:/workbench/` 或 `D:/tools/zhiji-workbench/`，**避免含中文、空格、`&` 等特殊字符**。

---

## 二、首次使用：填两件事

`.env` 由 `npm run dev` 首次跑时自动生成（或手动 `node scripts/setup-env.mjs`）：

| 项 | 说明 |
|---|---|
| `OPENAI_API_KEY` | 同事**各自**填自己的 DeepSeek Key（去 <https://platform.deepseek.com> 申请，¥1 起充） |
| `PERSONAL_DASHBOARD_VAULT_ROOT` | 团队共享 Vault 绝对路径（见第三节） |

任何时候改 `.env` 后重启 `npm run dev` 生效。

---

## 三、共享 Vault（团队知识库）

团队共享同一份 Vault——车型参数、最新权益、广告法、爆文库都从这读。由**维护者**先用下面命令初始化一次：

```bash
node scripts/init-vault.mjs "D:\团队共享\智己知识库"
```

再把这个目录放进飞书知识库 / 网盘同步盘 / 局域网共享盘。**每位同事**在 `.env` 里把 `PERSONAL_DASHBOARD_VAULT_ROOT` 指向同一位置即可。

> 脚本**不覆盖任何已存在文件**，重复运行只补缺失目录，可放心多次执行。

---

## 四、常见问题

- **装依赖慢** → 在工作台根目录建一个 `.npmrc` 加一行 `registry=https://registry.npmmirror.com`
- **打开白屏** → 确认 `npm run dev` 那个命令行窗口没关、没报错；浏览器刷新；不行就重跑
- **内容生成没反应 / 提示未配置 Key** → `.env` 里的 `OPENAI_API_KEY` 没填或填错，改完重启
- **「车型资料库」或「创作知识库」加载失败 / 显示「请检查本地 Workbench 服务」** → 这是本机飞书（lark-cli）未登录，页面顶部会有专门的登录引导横幅，按提示在新开终端跑 `lark-cli auth login`，浏览器弹窗完成授权后点页面里的「重新检测」即可。其它四个模块（内容生成 / 审核 / 每日热点 / 社媒洞察）不依赖飞书，能正常用
- **同事没有飞书账号 / 不想登录飞书** → 把 `server/feishu-sources.json` 里全部 `"enabled"` 改成 `false`，车型资料库走本地 `public/car-reference/` 兜底图
- **同事没有 DeepSeek 账号** → 也可以改接 OpenAI：在 `.env` 里把 `OPENAI_BASE_URL` 改成 `https://api.openai.com/v1`，`OPENAI_MODEL` 改成 `gpt-4o-mini`

---

## 五、维护者：升级发版

1. 改完代码，本地 `npm run dev` 自测
2. 在 GitHub 仓库点 Compare & pull request / 直接 push main
3. 同事重新跑安装 prompt 即可升级（或者 `git pull && npm install`）

---

## 六、维护者：仓库准备（一次性）

把 Workbench 拆成独立 git 仓库（不包含团队私有 Vault `个人知识库/`），推 GitHub 公开仓库：

```bash
cd "C:/Users/28691/WorkBuddy/person_dashboard/Workbench"
# 在 GitHub 网页上先建一个空仓库（如 zhiji-workbench，public），得到 https://github.com/你/zhiji-workbench.git
# 然后本地：
git init -b main
git add -A
git commit -m "Initial public release: 智己小红书运营工作台"
git remote add origin https://github.com/你/zhiji-workbench.git
git push -u origin main
```

`.gitignore` 已经把 `.env` / `node_modules` / `data/` / `dist-package/` / `electron/release/` / 个人 Vault 等都排除，不会把任何密钥或私数据推到公开仓库。

跑前再 `npm run privacy:scan` 扫一遍保险。

---

## 七、进阶：备选分发方式

### 7.1 一键运行包（zip 解压即用，不依赖 WorkBuddy）

适合团队里有人不熟命令行：

```bash
node scripts/build-package.mjs --with-node "<node-v22.x-win-x64.zip>"
```

产出 `dist-package/工作台一键运行包.zip`（约 37MB，含便携 Node），同事解压后双击 `start-workbench.bat`。

### 7.2 桌面安装版（双击图标的 .exe）

适合想让同事用得更原生：

```bash
cd electron
npm install
npm run dist
```

产出 `electron/release/智己运营工作台 Setup x.x.x.exe`。首次对外分发建议做代码签名（否则 Windows 弹 SmartScreen）。
