# 网页版部署说明（腾讯云 CloudBase）

## 当前线上地址

**https://zhiji-d4g0etkrwf7e7d1de-1494775572.tcloudbaseapp.com/**

- 环境 ID：`zhiji-d4g0etkrwf7e7d1de`（ap-shanghai，体验版）
- 托管方式：CloudBase **静态网站托管**
- 已发布 435 个文件 = 前端 + `public/` 全量镜像（车型图 / 创作素材 / 本地缓存图）+ 9 条路由的 index.html 副本

---

## 同事怎么用（直接转发这段话）

1. 打开 https://zhiji-d4g0etkrwf7e7d1de-1494775572.tcloudbaseapp.com/
2. 左侧最下面「设置 → API Key」填自己的 DeepSeek Key（DeepSeek 控制台申请）
3. 回「内容生成」选车型 → 生成

不用装 Node、不用拉代码、不用连内网。Key 只存在他自己的浏览器里，别人看不到，也不经过我们服务器。

---

## 你后续怎么更新

一条命令：

```bash
npm run deploy:cloudbase
```

它会先跑 `build:hosted`（生成快照 + 打包），再把 `dist/client` 推到 CloudBase。

| 想更新什么 | 怎么做 |
| --- | --- |
| 改功能 / 改界面 | 改代码 → `npm run deploy:cloudbase` |
| 更新车型参数 | 改 `个人知识库/wiki/` → `npm run deploy:cloudbase` |
| 更新每日热点 / 社媒洞察 | 确保本地 5173 在跑 → `WORKBENCH_SNAPSHOT_API=http://127.0.0.1:5173 node scripts/build-hosted-snapshot.mjs` → `npm run deploy:cloudbase` |
| 新增飞书素材图 | `npm run deploy:media`（只传图，45 秒，不用打包） |
| 改代码 / 改界面（图没变） | `npm run deploy:slim`（只传代码，~20MB） |
| 代码和图都要发 | `npm run deploy:cloudbase`（全量，含图 ~100MB） |

换环境时：`CLOUDBASE_ENV_ID=你的环境 npm run deploy:cloudbase`。

### 图片单独更新（推荐日常用这条）

```bash
npm run deploy:media              # 先同步飞书最新图，再只上传图片目录
npm run deploy:media -- --no-sync # 跳过同步，直接上传本地已有的图
```

图和代码分开之后，改一张图不用再重新打包 + 重传 100MB：实测 **145 张图 45 秒**传完。
图片传到托管上的路径和站点里的完全一致（都是根目录），所以**前端不用改、不用配 CDN、没有跨域问题**。

### 每天自动同步（一次配好，之后全自动）

已经备好脚本 `scripts/media-autosync.bat`，在你自己的终端（PowerShell / CMD，不用管理员）里跑一次：

```
schtasks /Create /TN ZhijiWorkbenchMediaSync /TR C:\Users\28691\WorkBuddy\person_dashboard\Workbench\scripts\media-autosync.bat /SC DAILY /ST 22:00 /F
```

- 每天 22:00 自动：同步飞书最新图 → 上传图目录
- 日志：`Workbench/hosting-media-auto.log`
- 依赖：同步飞书那一步要本地工作台在跑（装过自启动的话没问题），工作台没起来就自动跳过同步、只传已有图
- 想改时间：把 `/ST 22:00` 换成别的；想删掉：`schtasks /Delete /TN ZhijiWorkbenchMediaSync /F`

**网页版现在是「本地全量镜像」**：`public/` 里有什么就发什么，线上和本地看到的一样。
想少传点（比如不发布本地缓存图 `car-reference`、`ima-materials`，能省 ~96MB）：

```bash
HOSTED_PUBLIC_EXCLUDE=car-reference,ima-materials npm run deploy:cloudbase
```

构建日志会打印实际镜像了多少 MB，方便判断上传耗时。

> CDN 通常几分钟内刷新。看不到更新就用无痕窗口，或 `curl -H "Cache-Control: no-cache" <地址>`。

---

## 两个实现细节（改代码时别踩）

**1. 子路由靠「路由副本」而不是云端配置**
静态托管默认只认真实文件，`/settings` 会 404。构建时会为每条路由生成一份
`dist/client/<路由>/index.html`（见 `scripts/vite-plugin-hosted-public.mjs` 的 `SPA_ROUTES`）。
**新增页面路由时，记得把路由名加进 `SPA_ROUTES`**，否则线上刷新会 404。

**2. 构建必须用 vite 的 JS API，不能用子进程跑 vite CLI**
`vite build` 打完包进程不退出（esbuild 等服务还挂着句柄），实测卡死 34 分钟无任何输出，
后面的 `hosting deploy` 永远等不到执行。所以 `scripts/build-hosted.mjs` 走
`import("vite").build()`，结束后 `process.exit(0)`。**别把它改回 `spawn(viteCli, ["build"])`。**

**3. Windows 上不能用 `spawn(cli, args, { shell: true })` 调 cloudbase CLI**
CLI 是 `#!/bin/sh` 脚本，`shell:true` 在 Windows 走 cmd.exe 会挂死。
`scripts/deploy-cloudbase.mjs` 里改成 `spawn("bash", [cli, ...args])`。

**4. 大模型调用走浏览器直连**
没有后端，DeepSeek Key 由使用者填在自己浏览器里。如果直连被 CORS 拦，
代码会自动改走同域 `/api/llm-proxy` —— 但静态托管没有后端，这个代理不存在，
所以目前**完全依赖 DeepSeek 允许跨域**。若同事反馈"填了 Key 还是失败"，
先让他点设置页的「测试连接」看具体报错。

---

## 安全提醒（建议尽快做）

现在这个域名是**公网可访问**的，里面有车型参数、权益和创作知识库内容。
收口方式（任选）：

- CloudBase 控制台 → 环境 → **静态网站托管** → 访问控制 / 防盗链
- 或接一层**云开发身份认证**（改代码，成本较高）
- 最省事：先在控制台把默认域名关掉，改用「仅内网/指定 IP 可访问」的自定义域名

另外：体验版环境 2027-03-23 到期，届时需要在控制台续期或转按量付费。
